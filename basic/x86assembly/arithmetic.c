#include <stdio.h>

int main() {
	int a = 420;
	int b = 69;

	int sum = a + b;
	int diff = a - b;
	int prod = a * b;

	printf("sum: %d\n", sum);
	printf("diff: %d\n", diff);
	printf("prod: %d\n", prod);

	return 0;
}
